# Copiar a secrets.toml en local (NO subir a Git).
# En Render / servidor: variable de entorno BI_PASSWORD (preferida)
# o Secrets de Streamlit.

[auth]
password = "cambiar-esta-clave-institucional"

# Producción ministerial (alternativa a BI_ENV=production):
# BI_REQUIRE_AUTH=1
# BI_PASSWORD=...
