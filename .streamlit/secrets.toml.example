# Copiar este archivo a secrets.toml en local (NO se sube a Git).
# En Render: Environment → Secrets, o Streamlit Cloud secrets.

# Autenticación simple del tablero (descomentar para activar)
# [auth]
# password = "cambiar-esta-clave"

# Alternativa vía variable de entorno en Render: BI_PASSWORD
